{
  "name": "tonalidades",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:gas": "bash scripts/build-gas-js.sh",
    "serve": "python3 -m http.server 8080 --bind 0.0.0.0 --directory .",
    "test": "node --test"
  }
}
